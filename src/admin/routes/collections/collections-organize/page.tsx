import { RouteFocusModal } from '../../../components/modals/route-focus-modal';
import OrganizeCollectionForm from '../../../components/organize-collections-form';

const CollectionOrganize = () => {
	return (
		<RouteFocusModal>
			<OrganizeCollectionForm />
		</RouteFocusModal>
	);
};

export default CollectionOrganize;
